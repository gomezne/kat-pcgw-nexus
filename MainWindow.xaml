<Window x:Class="kat_pcgw_nexus.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kat_pcgw_nexus"
        mc:Ignorable="d"
        Title="KAT PCGW Nexus" SizeToContent="WidthAndHeight" ResizeMode="CanMinimize" WindowStyle="ToolWindow">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Margin="10,5,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Label Content="Set address for discovery by Nexus Gateway:" 
                   Grid.Row="0" 
                   Grid.Column="0" 
                   Grid.ColumnSpan="2"  
                   Margin="-5,0,0,1"/>
            <ComboBox x:Name="IpAddressComboBox" 
                      Grid.Row="1" 
                      Grid.Column="0" 
                      Grid.ColumnSpan="2"
                      Width="250"
                      Margin="0,0,0,5">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding AddressStr}" TextWrapping="NoWrap" />
                            <TextBlock Text=" (" />
                            <TextBlock Text="{Binding AdapterName}" TextWrapping="NoWrap" />
                            <TextBlock Text=")" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <Button x:Name="ViewLogBtn" 
                Content="View Log"
                Grid.Row="2" 
                Grid.Column="0" 
                Margin="0,0,2.5,0" 
                Click="ViewLogBtn_Click"/>
            <Button x:Name="CloseBtn" 
                Content="Close"
                Grid.Row="2" 
                Grid.Column="1"
                Padding="5" 
                Margin="2.5,0,0,0" 
                Click="CloseBtn_Click" 
                Grid.ColumnSpan="2"/>
        </Grid>
        <StatusBar x:Name="statusBar" 
                   Grid.Row="1"
                   Grid.Column="0">
            <StatusBarItem HorizontalAlignment="Left">
                <TextBlock x:Name="statusBarMsg" Text="No connected KAT devices"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="statusBarVersion"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
