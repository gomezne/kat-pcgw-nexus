<Window x:Class="kat_pcgw_nexus.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kat_pcgw_nexus"
        mc:Ignorable="d"
        Title="Log" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox x:Name="JsonTextBox" 
                Grid.Row="0"
                HorizontalAlignment="Stretch" 
                VerticalAlignment="Top" 
                TextWrapping="NoWrap" 
                IsReadOnly="True" 
                HorizontalScrollBarVisibility="Auto" 
                Margin="0" 
                Width="Auto"
                Height="Auto" />
        <TextBox x:Name="ReceivedDataTextBox" 
                 HorizontalAlignment="Stretch" 
                 VerticalAlignment="Stretch" 
                 Grid.Row="1"
                 TextWrapping="Wrap" 
                 IsReadOnly="True" 
                 Margin="0,0,0,5" 
                 VerticalScrollBarVisibility="Auto" 
                 HorizontalScrollBarVisibility="Auto" 
                 ScrollViewer.CanContentScroll="True"/>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Button x:Name="ClearLogBtn" 
                    Content="Clear Log"
                    VerticalAlignment="Bottom" 
                    Click="ClearLogBtn_Click"
                    Margin="0,0,2.5,0" />
            <Button x:Name="CloseLogBtn" 
                    Content="Close Log"
                    Grid.Column="1"
                    VerticalAlignment="Bottom" 
                    Click="LogWindow_Closed" 
                    Margin="2.5,0,0,0" />
        </Grid>
    </Grid>
</Window>
